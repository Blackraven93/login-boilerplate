export default function Page({}) {
  /**
   * Model
   * 1. name, phone, birth_date,
   *  - 중복 확인
   * 2. Authentication(SMS)
   * 3. password
   * 4. profile image
   */
  return <main>login</main>;
}
